<div class="container mt-4">

  <h1 class="font-weight-bold text-danger mb-4">La casa del Mojito </h1>

  <ul> <% @cocktails.each do |element| %>
        <li class="list-group-item" ><%= link_to element.name, cocktail_path(element) %>
            <%= element.description  %>
            <%= element.photo %>
            <%= link_to "Delete", cocktail_path(element),
              method: :delete,
              data: { confirm: "Are you sure?" } %></li>
        <% end %>
    </ul>

  <%= cl_image_tag("orfyxglkalroyck8ucl2"), width: 400, height: 300, crop: :fill) %>

</div>

